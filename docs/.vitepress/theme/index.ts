// Extend the default VitePress theme with the musd-kit brand (custom.css). Light-only canvas,
// red+white identity, self-hosted Satoshi + Geist Mono, the shared warm code theme.
import DefaultTheme from 'vitepress/theme'
import './custom.css'

export default DefaultTheme
