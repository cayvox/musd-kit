// @musd-kit/react — public surface.
//
// Phase 0 is scaffold only; the hook set arrives in Phase 8. This package
// provides no wagmi provider of its own — it consumes the context Passport (or
// any wagmi setup) already established. See `docs/04-react-api.md`.
export {}
